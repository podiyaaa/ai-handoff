# Source & build tools — only dist/ ships
src/
out/
esbuild.js
tsconfig.json
tsconfig.test.json
.eslintrc.json

# Standalone webview dev harness (mock bridge + browser test page) —
# dev-only, never loaded by the real extension. media/webview/*.js is the
# actual shipped webview code and stays included.
webview-ui/

# Dev / repo housekeeping
.gitignore
.gitattributes
setup-github.sh
CLAUDE.md
CONTRIBUTING.md
NEXT-STEPS.md

# Test artifacts
*.xml
*.vsix
coverage/
.nyc_output/

# Dependencies (extension is bundled via esbuild)
node_modules/

# Misc
.DS_Store
*.log
.claude/
